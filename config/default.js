const config = {
  // 启动端口
  port: 1200,

  // 数据库配置
  database: {
    DATABASE: 'node',
    USERNAME: 'root',
    PASSWORD: '123456',
    PORT: '3306',
    HOST: '39.106.12.146'
  }
}

module.exports = config
